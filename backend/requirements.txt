# Core dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
yt-dlp>=2025.1.15
pydantic>=2.9
pydantic-settings>=2.7
python-multipart>=0.0.19
sse-starlette>=2.2
httpx>=0.28

# Test dependencies
pytest>=8.3
pytest-asyncio>=0.25

# Audio separation dependencies
demucs>=4.0.1
# Torch CPU only - use extra index url for CPU builds
# Install with: pip install --index-url https://download.pytorch.org/whl/cpu torch
# torchaudio needs to match torch version
torchaudio>=2.0.0
aiofiles>=23.0.0

# BPM detection dependencies
numpy>=1.24.0
madmom>=0.16.1
librosa>=0.10.0
